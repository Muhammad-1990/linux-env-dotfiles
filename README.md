# linux-env-dotfiles

stow will apply symlink one level up to override that you must use the -t flag. This is only necessary if your repo does not reside in the root of your $HOME

```stow -t ~ [PACKAGENAME]```
